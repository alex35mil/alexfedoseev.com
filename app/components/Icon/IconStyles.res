include %css(
  let sm = 12
  let md = 16
  let lg = 24

  let icon = css`
    transition-duration: ${Transition.fast};
    transition-timing-function: ${Transition.timingFunction};
  `

  let smSize = css`
    width: ${sm}px;
    height: ${sm}px;
  `

  let mdSize = css`
    width: ${md}px;
    height: ${md}px;
  `

  let lgSize = css`
    width: ${lg}px;
    height: ${lg}px;
  `
)
