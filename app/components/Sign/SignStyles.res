include %css(
  let sign = css`
    fill: ${Theme.signColor};
    transition-property: fill;
    transition-duration: ${Transition.moderate};
    transition-timing-function: ${Transition.timingFunction};
  `
)
